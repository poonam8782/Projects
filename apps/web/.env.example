# Frontend environment variables
# Copy this to .env in the apps/web directory
# Or use the root .env file (recommended for monorepo)

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Backend Configuration
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# NEXT_PUBLIC_* variables are exposed to the browser
# Never put sensitive keys (service role key) in NEXT_PUBLIC_* variables
# For production, set these in Vercel environment variables

# Landing Page Configuration
# External purchase/checkout URL used by the landing page Purchase button.
# If unset, the Purchase button will show a "Coming soon" toast instead of navigating.
# This can be a Stripe payment link, Lemon Squeezy checkout, or any external URL.
# Examples:
# NEXT_PUBLIC_PURCHASE_URL=https://buy.stripe.com/test_xxxxx
# NEXT_PUBLIC_PURCHASE_URL=https://yoursite.lemonsqueezy.com/checkout/buy/xxxxx
NEXT_PUBLIC_PURCHASE_URL=https://buy.stripe.com/your-payment-link
